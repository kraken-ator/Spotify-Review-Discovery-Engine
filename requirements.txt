# Spotify Discovery Review Intelligence Engine — requirements.txt
#
# Install with:
#   pip install -r requirements.txt
#
# Notes:
# - sentence-transformers and umap-learn are used if available (better
#   embeddings / 2D layout); the pipeline degrades gracefully to TF-IDF
#   (scikit-learn) and PCA if they're missing or too heavy for your
#   environment. Keep them installed for the quality you'll actually want
#   to submit, but the app will not crash without them.
# - hdbscan is used for natural cluster-count discovery; if it's not
#   installed, clusterer.py falls back to KMeans with a silhouette-selected
#   k (still NOT a hardcoded cluster count).
# - tweepy is only needed if you enable the live Twitter API toggle, which
#   requires a paid Twitter/X developer tier (free tier returns 402).

# --- Core app ---
streamlit>=1.36
pandas>=2.0
numpy>=1.24
plotly>=5.20

# --- Scraping ---
requests>=2.31
beautifulsoup4>=4.12
google-play-scraper>=1.2.6
tweepy>=4.14          # only needed for the live Twitter API toggle
python-dotenv>=1.0
tqdm>=4.66

# --- Embeddings / dimensionality reduction / clustering ---
scikit-learn>=1.3
sentence-transformers>=2.6   # optional but recommended — see note above
umap-learn>=0.5              # optional but recommended — see note above
hdbscan>=0.8.33              # optional but recommended — see note above

# --- LLM synthesis ---
groq>=0.9.0
